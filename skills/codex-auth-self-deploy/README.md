# codex-auth-self-deploy

Skill for bootstrapping OpenClaw `openai-codex` auth plugin deployment on fresh agents/nodes.

Main command:

```bash
bash /root/.openclaw/workspace/tools/openai-codex-auth-plugin/scripts/self-deploy.sh
```
